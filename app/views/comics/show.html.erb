<%= provide(:title, @comic.title.html_safe) unless @news %>

<%= render 'layouts/controls', comic: @comic, category: @category, top: true %>

<ul class="comics">
	<%= render 'comics/comic', comic: @comic %>
</ul>

<%= render 'layouts/controls', comic: @comic, category: @category %>