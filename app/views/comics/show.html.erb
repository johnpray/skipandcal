<%= provide(:title, @comic.title) %>

<%= render 'layouts/controls', comic: @comic, category: @category, top: true unless @news %>

<ul class="comics">
	<%= render 'comics/comic', comic: @comic %>
</ul>

<%= render 'layouts/controls', comic: @comic, category: @category %>